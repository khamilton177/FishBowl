<%= form_for([@post, @post.comments.build]) do |comment_f| %>
  <p>
    <%= comment_f.text_area :content, class: "prof_f" %>
  </p>
  <p>
    <%= comment_f.submit "Save", class: "save_f" %>
  </p>
<% end %>
